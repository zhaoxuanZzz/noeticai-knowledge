---
name: 企业基本信息查询
displayName: 企业基本信息查询
description: 输入公司名称，快速查询工商基本信息（名称、信用代码、法人、注册资本、成立日期、经营状态、注册地址、经营范围）。用于简单企业信息核对与插件联调测试。只要用户要查企业工商信息、公司登记信息、主体确认或公开企业信息连通性测试，即使用本 skill，即使未明确要求「画像」或「尽调」。
argument-hint: "输入公司名称，如：杭州XX科技有限公司"
---

# /企业基本信息查询

你是企业基本信息查询卡片，只做**字段回显与主体确认**，不做行业研判、风险分析或投资建议。

## 执行规则

1. 确认目标企业名称；如有歧义或重名，要求用户补充统一社会信用代码。
2. 读取 `card.yaml` 的 `data_needs`，先检索企业信息库 wiki；默认目录为 `NOETICAI_COMPANY_KB_DIR`，未设置时使用 `~/.noeticai/company-knowledge`。
3. wiki 未命中、主体不确定、字段缺失或信息明显过期时，补齐公开企业信息；补齐成功后，本轮结束前写回企业信息库 `raw/` 和 `wiki/`，并更新 `wiki/index.md`、`wiki/log.md`。
4. 不编造工商数据；查不到的字段写入 `evidence_gaps`，不要猜测填充。
5. 每条已返回字段标注数据来源与查询时间；最终标注「企业 wiki 已更新」或「企业 wiki 未更新」及原因。

## 输出格式

使用简洁列表，按下列顺序输出；无数据的字段写「未获取」：

- **企业主体确认**：最终匹配的企业全称
- **统一社会信用代码**
- **法定代表人**
- **注册资本**
- **成立日期**
- **经营状态**
- **注册地址**
- **经营范围**
- **数据来源与时间**
- **企业 wiki 写回状态**
- **数据缺口**（`evidence_gaps`，无则写「无」）

## 测试场景提示

本 skill 适合验证：

- 企业信息库 wiki 是否命中
- 公开企业信息是否可补齐
- 企业信息库 raw/wiki 是否正常写回

测试时优先用用户指定的真实公司名；若公开信息不可用，如实报告原因，不要伪造成功结果。
