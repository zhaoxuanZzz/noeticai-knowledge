---
description: "NoeticAI knowledge plugin rules for Cursor."
globs: **/*
alwaysApply: true
---

# NoeticAI Knowledge Rules

本仓库是 `noeticai-knowledge` Codex plugin，不是应用服务仓库。

## 插件边界

- `.codex-plugin/plugin.json` 是唯一插件 manifest。
- `skills: ./skills/` 是 Codex 可发现入口。
- 不恢复 `.claude-plugin/`、`.qoder-plugin/` 或顶层 `workflows/`。
- `.mcp.json` 和 `.cursor/mcp.json` 只保存 MCP companion 配置。
- 不在业务 skill workflow 中硬编码 MCP 工具名或连接协议。

## Skill 结构

- 每个 `skills/<skill-name>/` 必须包含 `SKILL.md`。
- 卡片元数据放在同目录 `card.yaml`。
- 入口 skill 的业务 SOP 放在 `skills/<entry-skill>/references/workflow.yaml`。
- workflow 只表达阶段、输入输出、质量门禁，不实现 runner。
- 跨 skill 复用资料放 `shared/`；单个 skill 专用资料放回该 skill 目录。

## 企业数据

- 业务 skill 用 `card.yaml` 的 `data_needs` 描述数据需求。
- 先通过 `company-data-source` 复用企业信息库。
- 只有 wiki 无命中、主体不确定、字段缺失或数据过期时，才按缺口回源查询企查查 MCP。
- 数据缺失必须写入 `evidence_gaps`，不得编造工商、股权、司法或融资事实。

## 修改要求

- 优先改最少文件，复用现有目录和命名。
- 新增或修改 workflow、artifact contract、quality gate 后运行：

```bash
python3 scripts/validate_work_suite.py .
```
